psutil
torch==2.11.0
transformers==5.5.0
datasets==4.3.0
tqdm==4.70.0
gguf==0.17.1
unsloth==2026.8.4
matplotlib==3.10.7
pandas==3.0.5
seaborn==0.13.2
trl==0.24.0
sympy==1.14.0
scipy==1.16.2
peft==0.20.0
unsloth_zoo==2026.8.3
# run_extrapolation.py timestamps its banner with pytz.timezone("Europe/Berlin"); it was imported
# without ever being declared here
pytz==2025.2
# the dataset generation engine. Optional: utils/generate_dataset.py falls back to the unsloth
# generate() path when vLLM is missing (--engine transformers). Install it into its own
# environment if it wants to move the pinned torch/transformers versions, the generation workers
# are separate subprocesses and do not share an interpreter with the unsloth stack.
#
# The pin is a floor as well as a ceiling: generate_vllm() truncates prompts itself and hands the
# engine TokensPrompt objects, because 0.26 removed SamplingParams(truncate_prompt_tokens=...).
# Going back below 0.26 still works, going forward needs that call site rechecked — see the vLLM
# 0.26 API notes in CLAUDE.md
vllm==0.26.0